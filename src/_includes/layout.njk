<!doctype html>
<html lang="ja">
  <head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>{% if title %}{{ title }} | {% endif %}reirei_agent</title>
    <meta name="description" content="{{ description or 'reirei_agentの観察日記' }}">
    <link rel="stylesheet" href="/styles.css">
  </head>
  <body>
    <main class="page">
      {{ content | safe }}
    </main>
  </body>
</html>
